/**
 * Файл для ваших собственных реплик ассистента (голоса).
 * Вы можете изменять, удалять или добавлять сюда любые реплики.
 * Они будут случайным образом появляться при нажатии на ассистента на главном экране.
 */

export interface CustomQuote {
  // Текст реплики на русском языке (для ru_RU и ru_RU_CN)
  ru: string;
  // Текст реплики на английском языке (опционально, для других локалей)
  en?: string;
  // ID оператора, для которого предназначена реплика (опционально).
  // Например: 'char_002_amiya' (Амия), 'char_1033_w_2' (Wis'adel).
  // Если не указан, реплика может появиться у любого активного ассистента.
  operatorId?: string;
}

export const CUSTOM_QUOTES: CustomQuote[] = [
  {
    ru: "Приветствую, доктор! zoot-archive опять подвергся изменениям. Чтобы прочитать историю, нажмите на кнопку терминала.",
    en: "Greetings, Doctor! zoot-archive has undergone changes again. To read the story, press the terminal button."
  }
];
